return {
    root_markers = { "pyrightconfig.json", ".git", "pyproject.toml" },
    settings = {
        basedpyright = {
            config = {
                typeCheckingMode = "basic"
            }
        },
    }
}
